{
  "name": "dmsone-flex-mcp-server",
  "version": "0.1.1",
  "description": "Local MCP server for the DMS One Flex API (Claude Desktop / any MCP client). Wraps the same REST API as n8n-nodes-dmsone-flex.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "dmsone-flex-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist build",
    "sync-manifest": "npm run build && node scripts/sync-manifest.mjs",
    "version": "node scripts/sync-manifest.mjs && git add manifest.json",
    "bundle": "npm run build && node scripts/sync-manifest.mjs && rm -rf build && mkdir -p build/pkg && cp -R dist manifest.json icon.png package.json README.md INSTALL-WINDOWS.md INSTALL-MACOS.md .env.example build/pkg/ && (cd build/pkg && npm install --omit=dev --no-audit --no-fund) && npx -y @anthropic-ai/mcpb pack build/pkg \"dmsone-flex-$npm_package_version.mcpb\""
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "dms-one",
    "flex",
    "claude"
  ],
  "author": "DMS One",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "axios": "^1.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.20.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "prettier": "^3.9.6",
    "tsx": "^4.19.2",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.69.0"
  }
}
